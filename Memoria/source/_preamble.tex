\usepackage[utf8]{inputenc}
\usepackage{lscape}
\usepackage{pdflscape}
\usepackage{tabularx} % Extiende las capacidades de las tablas

\usepackage{rotating}
\usepackage{pdfpages}
\usepackage[spanish, es-tabla]{babel}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{csquotes}
\usepackage[left=30mm, top=30mm, bottom=30mm, right=30mm]{geometry}
\usepackage{sectsty}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{lipsum}
\usepackage{float}
\usepackage{afterpage}
\usepackage{nameref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{array}    % Para alinear columnas
\usepackage{longtable}
\usepackage{svg}
\usepackage{listings}
\usepackage{enumerate}
\usepackage{colortbl} % Paquete necesario para colorear celdas
\definecolor{lightgray}{gray}{0.9} % Definición del color gris tenue
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% \addto\captionsspanish{% Replace "english" with the language you use
%   \renewcommand{\contentsname}%
%     {Contenido}%
% }

\setlength{\headheight}{15pt}

\newcommand\blankpage{%
    \null
    \thispagestyle{empty}%
    \addtocounter{page}{-1}%
    \newpage}
    
% \usepackage[usefilenames,% Important for XeLaTeX
%   RMstyle={Medium,Semibold},
%   SSstyle={Medium,Semibold},
%   TTstyle={Medium,Semibold},
%   DefaultFeatures={Ligatures=Common}]{plex-otf} %
% \renewcommand*\familydefault{\sfdefault}

\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0.7pt}

\makeatletter

\newcommand\frontmatter{%
    \pagestyle{plain}
    \allsectionsfont{\normalfont\rmfamily\bfseries}
    \cleardoublepage
  %\@mainmatterfalse
  \pagenumbering{roman}}

\newcommand\mainmatter{%
    \allsectionsfont{\usefont{T1}{lmss}{b}{m}}
    \cleardoublepage
 % \@mainmattertrue
  \pagenumbering{arabic}
  \pagestyle{fancy}
  \rhead[]{SecureTicket: Solución B2B de reventa basada en contratos inteligentes Stellar/Soroban}
  \lhead[]{}
  \lfoot[]{}
  \rfoot[]{}
  \renewcommand{\headrulewidth}{0.7pt}
  \renewcommand{\footrulewidth}{0pt}}
\newcommand\backmatter{%
    \newpage
    \pagestyle{plain}
    \allsectionsfont{\normalfont\rmfamily\bfseries}
    \setcounter{section}{0}
    \renewcommand{\thesection}{\Alph{section}}
   }

\makeatother

\newcommand\blfootnote[1]{%
  \begingroup
  \renewcommand\thefootnote{}\footnote{#1}%
  \addtocounter{footnote}{-1}%
  \endgroup
}

\usepackage[backend=biber, style=ieee]{biblatex}
\addbibresource{bibliography.bib}

% Permite cortar cadenas largas (direcciones Stellar, hashes, contract IDs) en cualquier carácter para evitar desbordes de margen.
\usepackage{seqsplit}
\newcommand{\walletkey}[1]{{\small\texttt{\seqsplit{#1}}}}

% Espacio visible entre párrafos sin sangría inicial.
\usepackage{parskip}

% --- Control de desbordes de margen ---
% microtype mejora la justificación y reduce overfull hboxes con protrusión y expansión.
\usepackage[expansion=false]{microtype}
% Habilita hyphenación en fuentes monoespaciadas (\texttt) y en versalitas.
\usepackage[htt]{hyphenat}
% Permite separación más laxa entre palabras para preferir desbordes verticales pequeños sobre horizontales.
\sloppy
\emergencystretch=3em
\tolerance=2000
\hbadness=2000
% Permite romper línea después de cada guion bajo, slash o punto dentro de identificadores en \texttt.
\AtBeginDocument{%
  \let\SecTktOrigUnderscore\_%
  \renewcommand{\_}{\SecTktOrigUnderscore\allowbreak}%
}